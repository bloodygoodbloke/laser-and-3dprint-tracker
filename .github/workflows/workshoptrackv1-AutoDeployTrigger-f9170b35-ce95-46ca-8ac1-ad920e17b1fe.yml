name: Trigger auto deployment for workshoptrackv1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/workshoptrackv1-AutoDeployTrigger-f9170b35-ce95-46ca-8ac1-ad920e17b1fe.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.WORKSHOPTRACKV1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.WORKSHOPTRACKV1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.WORKSHOPTRACKV1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerfilePath: backend/Dockerfile
          registryUrl: ${{ secrets.WORKSHOPTRACKV1_REGISTRY_URL }}
          registryUsername: ${{ secrets.WORKSHOPTRACKV1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.WORKSHOPTRACKV1_REGISTRY_PASSWORD }}
          containerAppName: workshoptrackv1
          resourceGroup: ACA-WST
          imageToBuild: ${{ secrets.WORKSHOPTRACKV1_REGISTRY_URL }}/workshoptrackv1:${{ github.sha }}


